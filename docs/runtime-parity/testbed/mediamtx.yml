# MediaMTX: accept publishers from companion ffmpeg containers.
# Path names must match docker-compose.media.yml publishers.

logLevel: info

paths:
  people:
    source: publisher
  one_by_one:
    source: publisher
  all_others:
    source: publisher
