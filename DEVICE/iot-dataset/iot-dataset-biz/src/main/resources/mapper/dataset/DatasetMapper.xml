<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basiclab.iot.dataset.dal.pgsql.DatasetMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
     -->

    <!-- 1. 数据集列表查询（带分页和统计字段） -->
    <select id="getDatasetList" resultType="com.basiclab.iot.dataset.dal.dataobject.DatasetDO">
        SELECT
        d.*,
        <!-- 统计图片总数 -->
        (SELECT COUNT(*) FROM dataset_image di WHERE di.dataset_id = d.id) AS total_images,
        <!-- 统计已标注图片数 -->
        (SELECT COUNT(*) FROM dataset_image di WHERE di.dataset_id = d.id AND di.completed = 1) AS annotated_images
        FROM dataset d
        <where>
            <!-- 动态条件过滤 -->
            <if test="reqVO.datasetCode != null and reqVO.datasetCode != ''">
                AND d.dataset_code LIKE CONCAT('%', #{reqVO.datasetCode}, '%')
            </if>
            <if test="reqVO.name != null and reqVO.name != ''">
                AND d.name LIKE CONCAT('%', #{reqVO.name}, '%')
            </if>
            <if test="reqVO.datasetType != null">
                AND d.dataset_type = #{reqVO.datasetType}
            </if>
            <if test="reqVO.audit != null">
                AND d.audit = #{reqVO.audit}
            </if>
        </where>
        ORDER BY d.update_time DESC
    </select>

    <!-- 2. 数据集详情查询（包含统计字段） -->
    <select id="getDataset" resultType="com.basiclab.iot.dataset.dal.dataobject.DatasetDO">
        SELECT
            d.*,
            (SELECT COUNT(*) FROM dataset_image di WHERE di.dataset_id = d.id) AS total_images,
            (SELECT COUNT(*) FROM dataset_image di WHERE di.dataset_id = d.id AND di.completed = 1) AS annotated_images
        FROM dataset d
        WHERE d.id = #{datasetId}
    </select>

</mapper>