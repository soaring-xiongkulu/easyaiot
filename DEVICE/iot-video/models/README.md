# iot-video ONNX models

Canonical mid-control model directory after VIDEO cutover Phase1.

Expected files (not committed; copy from legacy `VIDEO/` or install pack):

- `face_rec.onnx`
- `face_det.onnx`
- `plate_detect.onnx`
- `plate_rec.onnx`
- `yolo26n-pose.onnx`

Override with `video.inference.models-dir` or env `VIDEO_MODELS_DIR`.
