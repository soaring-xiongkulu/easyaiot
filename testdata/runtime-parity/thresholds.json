{
  "_note": "默认阈值待产品拍板；case 可在 manifest 中覆盖。禁止为过关静默降低阈值。",
  "detect": {
    "bbox_iou_min": 0.5,
    "matched_bbox_ratio_min": 0.95,
    "count_tolerance": 0
  },
  "alarm": {
    "count_tolerance_per_minute": 1,
    "payload_field_ignore": ["timestamp", "received_at", "image_path", "correlation_id"],
    "smoke_allow_hook_bbox_drift": true,
    "_smoke_hook_bbox_note": "Oracle smoke alarm bbox is ONNX snapshot; live RUNTIME hook may fire on a different frame. G-4.1 requires hook golden keys + class/roi/count."
  },
  "track": {
    "id_mapping_ratio_min": 0.9,
    "switch_count_ratio_max": 1.1
  },
  "motion": {
    "motion_trigger_count_tolerance": 50,
    "infer_skip_ratio_min": 0.3,
    "infer_skip_ratio_max": 1.8
  },
  "overlay": {
    "p95_latency_ms_slack": 200,
    "min_drawn_count": 1,
    "_note": "cpp overlay P95 ≤ python P95 + slack; dual-queue 1:1 not required"
  },
  "stream": {
    "width_tolerance": 0,
    "height_tolerance": 0,
    "fps_tolerance": 3.0,
    "bitrate_kbps_min": 200,
    "bitrate_kbps_max": 8000,
    "gray_frame_max": 5,
    "min_pushed_ok": 10,
    "_note": "G-4.4 L_stream: ffprobe resolution/fps + encoder bitrate band; NVENC fallback is WARN"
  },
  "perf": {
    "alert_p95_ratio_max": 1.2,
    "alert_p95_ms_slack": 200,
    "fps_ratio_min": 0.9,
    "cpu_ratio_max": 0.85,
    "gpu_mem_ratio_max": 1.1,
    "rss_ratio_max": 0.8,
    "rss_absolute_mb_slack": 512
  },
  "schedule": {
    "patrol_interval_seconds": 5,
    "patrol_interval_tolerance_seconds": 4,
    "snap_cron_match_window_seconds": 5,
    "snap_min_slots": 1,
    "patrol_min_events": 2,
    "_note": "G-4.3 testbed uses shortened intervals (5s snap/patrol) vs production 30s"
  },
  "lifecycle": {
    "min_heartbeat_count_cpp": 1,
    "_note": "python golden may use smoke stubs with higher heartbeat_count; cpp must capture >=1 real heartbeat"
  }
}
