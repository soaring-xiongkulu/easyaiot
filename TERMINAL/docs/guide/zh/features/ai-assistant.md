# AI 助理

终端 内置自主 AI Agent，可在终端中独立规划并执行多轮 Shell 命令。

![AI 助理](/imgs/ai_assistant_light.webp)

## 核心能力

### 自主多轮执行

AI Agent 会：

1. **理解意图** — 分析您输入的自然语言需求
2. **制定计划** — 拆解为可执行的 Shell 命令步骤
3. **执行观察** — 在终端中运行命令，观察输出结果
4. **迭代调整** — 根据结果修正计划，继续执行直到完成任务

### LLM 提供商配置

支持 Anthropic 和 OpenAI 兼容 API，可添加多个模型并在使用中随时切换。

![AI 模型设置](/imgs/ai_model_light.webp)

**模型配置字段：**

| 字段 | 说明 |
|------|------|
| 名称 | 自定义显示名称，用于在模型列表中区分不同配置 |
| 协议 | 选择 **Anthropic** 或 **OpenAI** 协议，决定请求格式 |
| Base URL | API 端点地址，OpenAI 默认为 `https://api.openai.com/v1`，Anthropic 默认为 `https://api.anthropic.com` |
| User-Agent | 可选，自定义请求头中的 User-Agent 标识，提供 终端、Claude Code、Cursor 等预设，也支持手动输入 |
| API Key | API 认证密钥，以密码形式存储 |
| 模型 | 模型名称（如 `gpt-4o`、`claude-sonnet-4-20250514`），可通过「拉取模型列表」按钮从 API 自动拉取；下拉列表显示模型展示名，保存的是真实模型 ID |
| 测试连接 | 填写完 API Key 和模型后，点击「测试连接」验证配置是否正确 |
| 代理 | 可选，模型请求走指定代理或系统代理，详见[代理](/zh/features/proxy) |

**多模型管理：**

- 支持添加多个模型配置，每个独立设置协议、地址和密钥
- 在 AI 侧边栏顶部下拉菜单中切换当前使用的模型
- 设置中可编辑或删除已有模型配置
- 模型目录与 AI 自主交互轮次上限随云同步跨设备生效

### 执行模式

| 模式 | 说明 |
|------|------|
| 全部确认 | 每一条命令都需要手动确认 |
| 写指令确认 | 危险命令和文件写入操作需确认 |
| 高危指令确认 | 仅 rm、sudo 等高危命令需确认（默认） |
| 全部免确认 | 所有命令直接执行，无需确认 |

### 自主轮次上限

设置中可配置「AI 自主交互最大轮次」，限制 AI 自主执行命令与反馈的对话轮数上限，设为 0 表示不限制，默认 20。

### 终端集成

AI 助理与终端深度集成，命令直接在终端标签页中执行，无需手动复制粘贴。

**执行方式：**

1. 在 AI 对话框中输入自然语言需求（如"帮我检查磁盘使用情况"）
2. AI 分析需求后生成执行计划
3. 根据当前执行模式，可能需要您确认每条命令
4. 命令在目标终端中逐条执行，输出实时显示
5. AI 观察输出结果，自动调整后续步骤

**选择目标终端：**

- **跟随活动标签页**（默认）— AI 命令在当前激活的终端标签页中执行。切换标签页后，后续命令自动切换到新标签页
- **关联终端** — 点击 AI 面板顶部的 `[+]` 添加一个或多个关联终端，之后所有 AI 命令都在关联的终端中执行，不受切换影响

**AI 锁定：**

将 AI 锁定到某个面板后，该标签页与面板标题栏会显示警示色背景，AI 命令只会在此面板中执行，状态一目了然。

**分屏协作：**

拖拽 AI 对话面板到右侧区域，与终端形成左右分栏。一边查看 AI 的分析和计划，一边观察终端中的执行结果，互不遮挡。

## 会话管理

- **重命名会话** — 「最近会话」下拉中的会话支持重命名
- **导出为 Markdown** — 右键消息气泡，可将整个会话导出为 Markdown 文件
- **删除消息** — 右键消息气泡可仅删除该条消息，或删除它及其后所有消息（上下文回滚）

## 技能与命令

技能（Skills）和命令（Commands）用于沉淀可复用的工作流与提示词。在 AI 输入框中输入 `/`，即可从下拉列表中选择并插入，二者均以 `/名称` 形式出现。

![技能与命令管理](/imgs/skills_light.webp)

### 技能（Skills）

技能是一段可复用的命令行工作流 / SOP，AI 可在需要时自主调用。

- **自主调用** — AI 判断当前任务匹配某个技能时，会自动读取并按其步骤执行，无需手动触发
- **手动唤起** — 在输入框输入 `/名称` 手动插入技能
- **保存技能** — AI 也可在整理出可复用流程后，通过 `save_skill` 将其保存为新技能

**管理方式**（设置 → 技能与命令）：

- **新建** — 填写名称（kebab-case）、描述和指令正文创建
- **导入** — 支持拖入 `.md` / `.zip` / `.skill` 文件，或选择文件夹导入（含引用脚本的目录型技能）
- **启用 / 锁定** — 用开关控制是否生效；锁定后变为只读，防止误改误删
- **编辑 / 删除** — 随时修改描述与正文，或删除自建技能

::: warning 安全提示
技能会驱动 AI 执行终端命令，请配合执行模式谨慎确认。
:::

### 命令（Commands）

命令是带参数占位的可复用 Prompt 模板，在对话中通过 `/` 快速调用。

![使用技能与命令](/imgs/use_skills_light.webp)

- **参数占位** — 正文中使用 `$ARGUMENTS` 占位符接收 `/命令` 后面输入的参数；若未写占位符，参数会自动追加到末尾
- **参数提示** — 可为命令设置参数提示（如 `[env] [service]`），在下拉列表中展示用法
- **管理** — 与技能一致，支持新建、编辑、删除、启用、锁定

::: tip 相关内容
- [远程终端](/zh/connections/remote-terminal) —— 终端输入的智能补全
:::
